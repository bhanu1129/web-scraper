{
  "builds": [
    {
      "src": "index.js", // Replace with your main backend file name
      "use": "@vercel/node",
      "config": {
        "buildCommand": "npm install puppeteer" // Installs Puppeteer during build
      }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "/index.js" }]
}
